# 使用

```
java -jar readdex.jar <dexfile>		dexfile参数就是需要被解析的dex文件
```

# 目前版本

`v1.0`

# 说明

**readdex1.0**参考010解析结果和四哥解析工具源码而写的，打印所有字段，如果需要定向打印，可以自己下载源码，修改`paseFormatView.java`文件，如果觉得打印出来的数据格式不好看，可以修改`parseFormatParser.java`文件来美化输出